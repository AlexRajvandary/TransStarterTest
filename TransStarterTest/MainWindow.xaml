<Window x:Class="TransStarterTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TransStarterTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Car Sales Dashboard" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,10"/>

        <TabControl Grid.Row="1" 
                    ItemsSource="{Binding Tabs}" 
                    SelectedIndex="0">

            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Title}" />
                </DataTemplate>
            </TabControl.ItemTemplate>

            <TabControl.ContentTemplate>
                <DataTemplate>

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Группировка:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox Width="120"
                                      ItemsSource="{Binding DataContext.GroupingOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding GroupBy}"/>

                            <TextBlock Text="Агрегат:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                            <ComboBox Width="120"
                                      ItemsSource="{Binding DataContext.AggregateOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding AggregateBy}"/>

                            <TextBlock Text="Год:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                            <ComboBox Width="100"
                                      ItemsSource="{Binding DataContext.AvailableYears, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding YearFilter}"/>
                        </StackPanel>

                        <DataGrid Grid.Row="1" 
                                  ItemsSource="{Binding ReportData}" 
                                  AutoGenerateColumns="True"/>
                    </Grid>

                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Добавить отчет" 
                    Width="150" Height="30" 
                    Margin="0,0,10,0"
                    Command="{Binding AddTabCommand}"/>

            <Button Content="Export to Excel" 
                    Width="150" Height="30" 
                    Command="{Binding ExportCommand}"/>
        </StackPanel>
    </Grid>
</Window>
